# Run with `dart run ffigen --config ffigen.yaml`.
name: SherpaOnnxMacosBindings
description: |
  Bindings for `src/sherpa_onnx_macos.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/sherpa_onnx_macos_bindings_generated.dart'
headers:
  entry-points:
    - 'src/sherpa_onnx_macos.h'
  include-directives:
    - 'src/sherpa_onnx_macos.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
