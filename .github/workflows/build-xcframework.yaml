name: build-xcframework

on:
  push:
    branches:
      - master
      - macos-xcframework
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

  workflow_dispatch:

concurrency:
  group: build-xcframework-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_ios_xcframework:
    if: false
    name: ios tts-${{ matrix.with_tts }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest]
        with_tts: [ON, OFF]

    steps:
      - uses: actions/checkout@v4

      - name: Update version
        shell: bash
        run: |
          ./new-release.sh
          git diff .

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ios-tts-${{ matrix.with_tts }}

      - name: Build iOS shared
        # if: matrix.with_tts == 'ON'
        if: false
        shell: bash
        run: |
          export CMAKE_VERBOSE_MAKEFILE=ON
          ./build-ios-shared.sh

      - name: Build iOS
        if: matrix.with_tts == 'ON'
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          ./build-ios.sh

      - name: Build iOS (No tts)
        if: matrix.with_tts == 'OFF'
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          ./build-ios-no-tts.sh

      - name: Display artifacts
        if: matrix.with_tts == 'ON'
        shell: bash
        run: |
          ls -lh build-ios/*.xcframework.zip

      - name: Display artifacts
        if: matrix.with_tts == 'OFF'
        shell: bash
        run: |
          ls -lh build-ios-no-tts/*.xcframework.zip

      - name: Test xcframework
        if: matrix.with_tts == 'ON'
        shell: bash
        run: |
          mkdir -p /tmp/test-xcframework
          cd /tmp/test-xcframework
          unzip -o $GITHUB_WORKSPACE/build-ios/*.xcframework.zip -d .
          ls -lh sherpa-onnx.xcframework/

          # Find the library and headers
          lib_path=$(find sherpa-onnx.xcframework -name "*.a" | head -1)
          header_dir=$(find sherpa-onnx.xcframework -name "c-api.h" -exec dirname {} \; | head -1)

          echo "lib_path: $lib_path"
          echo "header_dir: $header_dir"

          # Compile and run
          clang -o version-c-api $GITHUB_WORKSPACE/c-api-examples/version-c-api.c \
            -I "$header_dir" \
            -L "$(dirname $lib_path)" \
            -l sherpa-onnx-c-api \
            -l onnxruntime \
            -lc++ \
            -Wl,-rpath,"$(dirname $lib_path)"

          ./version-c-api

      - name: Upload xcframework.zip
        if: matrix.with_tts == 'ON'
        uses: actions/upload-artifact@v4
        with:
          name: sherpa-onnx-ios-xcframework
          path: ./build-ios/*.xcframework.zip

      - name: Upload xcframework.zip (no tts)
        if: matrix.with_tts == 'OFF'
        uses: actions/upload-artifact@v4
        with:
          name: sherpa-onnx-ios-no-tts-xcframework
          path: ./build-ios-no-tts/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'k2-fsa' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-ios*/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'csukuangfj' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-ios*/*.xcframework.zip
          # repo_name: k2-fsa/sherpa-onnx
          # repo_token: ${{ secrets.UPLOAD_GH_SHERPA_ONNX_TOKEN }}
          # tag: v1.13.4

      # https://huggingface.co/docs/hub/spaces-github-actions
      - name: Publish to huggingface
        if: (github.repository_owner == 'csukuangfj' || github.repository_owner == 'k2-fsa') && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 20
          timeout_seconds: 200
          shell: bash
          command: |
            git config --global user.email "csukuangfj@gmail.com"
            git config --global user.name "Fangjun Kuang"

            rm -rf huggingface
            export GIT_CLONE_PROTECTION_ACTIVE=false
            GIT_LFS_SKIP_SMUDGE=1 git clone https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs huggingface

            cd huggingface

            cp -v ../build-ios*/*.xcframework.zip ./

            git status
            git lfs track "*.zip"

            git add .

            SHERPA_ONNX_VERSION=v$(grep "SHERPA_ONNX_VERSION" ../CMakeLists.txt | cut -d " " -f 2 | cut -d '"' -f 2)
            git commit -m "upload sherpa-onnx-${SHERPA_ONNX_VERSION} xcframework"

            git push https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs main


  build_macos_xcframework:
    if: false
    name: macos
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Update version
        shell: bash
        run: |
          ./new-release.sh
          git diff .

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: macos

      - name: Build macOS
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          ./build-macos.sh

      - name: Display artifacts
        shell: bash
        run: |
          ls -lh build-macos/*.xcframework.zip

      - name: Test xcframework
        shell: bash
        run: |
          mkdir -p /tmp/test-xcframework
          cd /tmp/test-xcframework
          unzip -o $GITHUB_WORKSPACE/build-macos/*.xcframework.zip -d .
          ls -lh sherpa-onnx.xcframework/

          # Find the library and headers
          lib_path=$(find sherpa-onnx.xcframework -name "*.a" | head -1)
          header_dir=$(find sherpa-onnx.xcframework -name "c-api.h" -exec dirname {} \; | head -1)

          echo "lib_path: $lib_path"
          echo "header_dir: $header_dir"

          # Compile and run
          clang -o version-c-api $GITHUB_WORKSPACE/c-api-examples/version-c-api.c \
            -I "$header_dir" \
            -L "$(dirname $lib_path)" \
            -l sherpa-onnx-c-api \
            -l onnxruntime \
            -lc++ \
            -Wl,-rpath,"$(dirname $lib_path)"

          ./version-c-api

      - name: Upload xcframework.zip
        uses: actions/upload-artifact@v4
        with:
          name: sherpa-onnx-macos-xcframework
          path: ./build-macos/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'k2-fsa' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-macos/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'csukuangfj' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-macos/*.xcframework.zip
          # repo_name: k2-fsa/sherpa-onnx
          # repo_token: ${{ secrets.UPLOAD_GH_SHERPA_ONNX_TOKEN }}
          # tag: v1.13.4

      # https://huggingface.co/docs/hub/spaces-github-actions
      - name: Publish to huggingface
        if: (github.repository_owner == 'csukuangfj' || github.repository_owner == 'k2-fsa') && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 20
          timeout_seconds: 200
          shell: bash
          command: |
            git config --global user.email "csukuangfj@gmail.com"
            git config --global user.name "Fangjun Kuang"

            rm -rf huggingface
            export GIT_CLONE_PROTECTION_ACTIVE=false
            GIT_LFS_SKIP_SMUDGE=1 git clone https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs huggingface

            cd huggingface

            cp -v ../build-macos/*.xcframework.zip ./

            git status
            git lfs track "*.zip"

            git add .

            SHERPA_ONNX_VERSION=v$(grep "SHERPA_ONNX_VERSION" ../CMakeLists.txt | cut -d " " -f 2 | cut -d '"' -f 2)
            git commit -m "upload sherpa-onnx-${SHERPA_ONNX_VERSION} macos xcframework"

            git push https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs main


  build_macos_shared_xcframework:
    name: macos-shared
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Update version
        shell: bash
        run: |
          ./new-release.sh
          git diff .

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: macos-shared

      - name: Build macOS shared
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          ./build-macos-shared.sh

      - name: Display build directory
        shell: bash
        run: |
          ls -lh build-macos-shared

      - name: Display artifacts
        shell: bash
        run: |
          ls -lh build-macos-shared/*.xcframework.zip
          unzip -l build-macos-shared/*.xcframework.zip

      - name: Test xcframework
        shell: bash
        run: |
          mkdir -p /tmp/test-xcframework
          cd /tmp/test-xcframework
          unzip -o $GITHUB_WORKSPACE/build-macos-shared/*.xcframework.zip -d .
          ls -lh sherpa-onnx.xcframework/

          # Find the library and headers
          lib_path=$(find sherpa-onnx.xcframework -name "*.dylib" | head -1)
          header_dir=$(find sherpa-onnx.xcframework -name "c-api.h" -exec dirname {} \; | head -1)

          echo "lib_path: $lib_path"
          echo "header_dir: $header_dir"

          # Compile and run
          clang -o version-c-api $GITHUB_WORKSPACE/c-api-examples/version-c-api.c \
            -I "$header_dir" \
            -L "$(dirname $lib_path)" \
            -l sherpa-onnx-c-api \
            -l onnxruntime \
            -lc++ \
            -Wl,-rpath,"$(dirname $lib_path)"

          ./version-c-api

      - name: Upload xcframework.zip
        uses: actions/upload-artifact@v4
        with:
          name: sherpa-onnx-macos-shared-xcframework
          path: ./build-macos-shared/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'k2-fsa' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-macos-shared/*.xcframework.zip

      - name: Release xcframework
        # if: github.repository_owner == 'csukuangfj' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-macos-shared/*.xcframework.zip
          repo_name: k2-fsa/sherpa-onnx
          repo_token: ${{ secrets.UPLOAD_GH_SHERPA_ONNX_TOKEN }}
          tag: v1.13.4

      # https://huggingface.co/docs/hub/spaces-github-actions
      - name: Publish to huggingface
        # if: (github.repository_owner == 'csukuangfj' || github.repository_owner == 'k2-fsa') && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 20
          timeout_seconds: 200
          shell: bash
          command: |
            git config --global user.email "csukuangfj@gmail.com"
            git config --global user.name "Fangjun Kuang"

            rm -rf huggingface
            export GIT_CLONE_PROTECTION_ACTIVE=false
            GIT_LFS_SKIP_SMUDGE=1 git clone https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs huggingface

            cd huggingface

            cp -v ../build-macos-shared/*.xcframework.zip ./

            git status
            git lfs track "*.zip"

            git add .

            SHERPA_ONNX_VERSION=v$(grep "SHERPA_ONNX_VERSION" ../CMakeLists.txt | cut -d " " -f 2 | cut -d '"' -f 2)
            git commit -m "upload sherpa-onnx-${SHERPA_ONNX_VERSION} macos-shared xcframework"

            git push https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs main
