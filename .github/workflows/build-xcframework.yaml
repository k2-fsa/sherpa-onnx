name: build-xcframework

on:
  push:
    branches:
      - master
      - macos-xcframework
    tags:
      - 'release-v[0-9]+.[0-9]+.[0-9]+*'

  workflow_dispatch:

concurrency:
  group: build-xcframework-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_ios_static_xcframework:
    name: ios-static
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Update version
        shell: bash
        run: |
          ./new-release.sh
          git diff .

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ios-static

      - name: Build iOS static
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          ./build-ios.sh

      - name: Display artifacts
        shell: bash
        run: |
          ls -lh build-ios/*.xcframework.zip

      - name: Test xcframework
        shell: bash
        run: |
          mkdir -p /tmp/test-xcframework
          cd /tmp/test-xcframework
          unzip -o $GITHUB_WORKSPACE/build-ios/*.xcframework.zip -d .
          ls -lh sherpa-onnx.xcframework/

          # Find the library and headers
          lib_path=$(find sherpa-onnx.xcframework -name "*.a" | head -1)
          header_dir=$(find sherpa-onnx.xcframework -name "c-api.h" -exec dirname {} \; | head -1)

          echo "lib_path: $lib_path"
          echo "header_dir: $header_dir"

          # Create expected include directory structure
          mkdir -p include/sherpa-onnx/c-api
          cp "$header_dir"/*.h include/sherpa-onnx/c-api/

          # Use onnxruntime from the build directory (iOS uses xcframework)
          ort_framework=$GITHUB_WORKSPACE/build-ios/ios-onnxruntime/onnxruntime.xcframework/ios-arm64

          # Compile and run (static lib, no rpath needed)
          clang -o version-c-api $GITHUB_WORKSPACE/c-api-examples/version-c-api.c \
            -I include \
            -L "$(dirname $lib_path)" \
            -F "$ort_framework" \
            -framework onnxruntime \
            -l sherpa-onnx-c-api \
            -lc++

          ./version-c-api

      - name: Upload xcframework.zip
        uses: actions/upload-artifact@v4
        with:
          name: sherpa-onnx-ios-static-xcframework
          path: ./build-ios/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'k2-fsa' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-ios/*.xcframework.zip
          tag: xcframework

      - name: Release xcframework
        # if: github.repository_owner == 'csukuangfj' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-ios/*.xcframework.zip
          repo_name: k2-fsa/sherpa-onnx
          repo_token: ${{ secrets.UPLOAD_GH_SHERPA_ONNX_TOKEN }}
          tag: xcframework

      # https://huggingface.co/docs/hub/spaces-github-actions
      - name: Publish to huggingface
        if: (github.repository_owner == 'csukuangfj' || github.repository_owner == 'k2-fsa') && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 20
          timeout_seconds: 200
          shell: bash
          command: |
            git config --global user.email "csukuangfj@gmail.com"
            git config --global user.name "Fangjun Kuang"

            rm -rf huggingface
            export GIT_CLONE_PROTECTION_ACTIVE=false
            GIT_LFS_SKIP_SMUDGE=1 git clone https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs huggingface

            cd huggingface

            cp -v ../build-ios/*.xcframework.zip ./

            git status
            git lfs track "*.zip"

            git add .

            SHERPA_ONNX_VERSION=v$(grep "SHERPA_ONNX_VERSION" ../CMakeLists.txt | cut -d " " -f 2 | cut -d '"' -f 2)
            git commit -m "upload sherpa-onnx-${SHERPA_ONNX_VERSION} ios-static xcframework"

            git push https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs main


  build_ios_shared_xcframework:
    name: ios-shared
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Update version
        shell: bash
        run: |
          ./new-release.sh
          git diff .

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ios-shared

      - name: Build iOS shared
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          ./build-ios-shared.sh

      - name: Display artifacts
        shell: bash
        run: |
          ls -lh build-ios-shared/*.xcframework.zip

      - name: Test xcframework
        shell: bash
        run: |
          mkdir -p /tmp/test-xcframework
          cd /tmp/test-xcframework
          unzip -o $GITHUB_WORKSPACE/build-ios-shared/*.xcframework.zip -d .
          ls -lh sherpa_onnx.xcframework/

          # Find the library and headers
          lib_path=$(find sherpa_onnx.xcframework -name "*.dylib" | head -1)
          header_dir=$(find sherpa_onnx.xcframework -name "c-api.h" -exec dirname {} \; | head -1)

          echo "lib_path: $lib_path"
          echo "header_dir: $header_dir"

          # Create expected include directory structure
          mkdir -p include/sherpa-onnx/c-api
          cp "$header_dir"/*.h include/sherpa-onnx/c-api/

          # Use onnxruntime from the build directory (iOS uses xcframework)
          ort_framework=$GITHUB_WORKSPACE/build-ios-shared/ios-onnxruntime/onnxruntime.xcframework/ios-arm64

          # Compile and run
          clang -o version-c-api $GITHUB_WORKSPACE/c-api-examples/version-c-api.c \
            -I include \
            -L "$(dirname $lib_path)" \
            -F "$ort_framework" \
            -framework onnxruntime \
            -l sherpa-onnx-c-api \
            -lc++ \
            -Wl,-rpath,"$(dirname $lib_path)"

          ./version-c-api

      - name: Upload xcframework.zip
        uses: actions/upload-artifact@v4
        with:
          name: sherpa-onnx-ios-shared-xcframework
          path: ./build-ios-shared/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'k2-fsa' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-ios-shared/*.xcframework.zip
          tag: xcframework

      - name: Release xcframework
        # if: github.repository_owner == 'csukuangfj' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-ios-shared/*.xcframework.zip
          repo_name: k2-fsa/sherpa-onnx
          repo_token: ${{ secrets.UPLOAD_GH_SHERPA_ONNX_TOKEN }}
          tag: xcframework

      # https://huggingface.co/docs/hub/spaces-github-actions
      - name: Publish to huggingface
        if: (github.repository_owner == 'csukuangfj' || github.repository_owner == 'k2-fsa') && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 20
          timeout_seconds: 200
          shell: bash
          command: |
            git config --global user.email "csukuangfj@gmail.com"
            git config --global user.name "Fangjun Kuang"

            rm -rf huggingface
            export GIT_CLONE_PROTECTION_ACTIVE=false
            GIT_LFS_SKIP_SMUDGE=1 git clone https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs huggingface

            cd huggingface

            cp -v ../build-ios-shared/*.xcframework.zip ./

            git status
            git lfs track "*.zip"

            git add .

            SHERPA_ONNX_VERSION=v$(grep "SHERPA_ONNX_VERSION" ../CMakeLists.txt | cut -d " " -f 2 | cut -d '"' -f 2)
            git commit -m "upload sherpa-onnx-${SHERPA_ONNX_VERSION} ios-shared xcframework"

            git push https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs main


  build_macos_static_xcframework:
    name: macos-static
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Update version
        shell: bash
        run: |
          ./new-release.sh
          git diff .

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: macos-static

      - name: Build macOS static
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          ./build-macos.sh

      - name: Display artifacts
        shell: bash
        run: |
          ls -lh build-macos/*.xcframework.zip

      - name: Test xcframework
        shell: bash
        run: |
          mkdir -p /tmp/test-xcframework
          cd /tmp/test-xcframework
          unzip -o $GITHUB_WORKSPACE/build-macos/*.xcframework.zip -d .
          ls -lh sherpa-onnx.xcframework/

          # Find the library and headers
          lib_path=$(find sherpa-onnx.xcframework -name "*.a" | head -1)
          header_dir=$(find sherpa-onnx.xcframework -name "c-api.h" -exec dirname {} \; | head -1)

          echo "lib_path: $lib_path"
          echo "header_dir: $header_dir"

          # Create expected include directory structure
          mkdir -p include/sherpa-onnx/c-api
          cp "$header_dir"/*.h include/sherpa-onnx/c-api/

          # Use onnxruntime from the build directory
          ort_lib=$GITHUB_WORKSPACE/build-macos/_deps/onnxruntime-src/lib

          # Compile and run (static lib, no rpath needed)
          clang -o version-c-api $GITHUB_WORKSPACE/c-api-examples/version-c-api.c \
            -I include \
            -L "$(dirname $lib_path)" \
            -L "$ort_lib" \
            -l sherpa-onnx-c-api \
            -l onnxruntime \
            -framework CoreFoundation \
            -framework Foundation \
            -lc++

          ./version-c-api

      - name: Upload xcframework.zip
        uses: actions/upload-artifact@v4
        with:
          name: sherpa-onnx-macos-static-xcframework
          path: ./build-macos/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'k2-fsa' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-macos/*.xcframework.zip
          tag: xcframework

      - name: Release xcframework
        # if: github.repository_owner == 'csukuangfj' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-macos/*.xcframework.zip
          repo_name: k2-fsa/sherpa-onnx
          repo_token: ${{ secrets.UPLOAD_GH_SHERPA_ONNX_TOKEN }}
          tag: xcframework

      # https://huggingface.co/docs/hub/spaces-github-actions
      - name: Publish to huggingface
        if: (github.repository_owner == 'csukuangfj' || github.repository_owner == 'k2-fsa') && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 20
          timeout_seconds: 200
          shell: bash
          command: |
            git config --global user.email "csukuangfj@gmail.com"
            git config --global user.name "Fangjun Kuang"

            rm -rf huggingface
            export GIT_CLONE_PROTECTION_ACTIVE=false
            GIT_LFS_SKIP_SMUDGE=1 git clone https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs huggingface

            cd huggingface

            cp -v ../build-macos/*.xcframework.zip ./

            git status
            git lfs track "*.zip"

            git add .

            SHERPA_ONNX_VERSION=v$(grep "SHERPA_ONNX_VERSION" ../CMakeLists.txt | cut -d " " -f 2 | cut -d '"' -f 2)
            git commit -m "upload sherpa-onnx-${SHERPA_ONNX_VERSION} macos-static xcframework"

            git push https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs main


  build_macos_shared_xcframework:
    name: macos-shared
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Update version
        shell: bash
        run: |
          ./new-release.sh
          git diff .

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: macos-shared

      - name: Build macOS shared
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          ./build-macos-shared.sh

      - name: Display build directory
        shell: bash
        run: |
          ls -lh build-macos-shared

      - name: Display artifacts
        shell: bash
        run: |
          ls -lh build-macos-shared/*.xcframework.zip
          unzip -l build-macos-shared/*.xcframework.zip

      - name: Test xcframework
        shell: bash
        run: |
          mkdir -p /tmp/test-xcframework
          cd /tmp/test-xcframework
          unzip -o $GITHUB_WORKSPACE/build-macos-shared/*.xcframework.zip -d .
          ls -lh sherpa-onnx.xcframework/

          # Find the library and headers
          lib_path=$(find sherpa-onnx.xcframework -name "*.dylib" | head -1)
          header_dir=$(find sherpa-onnx.xcframework -name "c-api.h" -exec dirname {} \; | head -1)

          echo "lib_path: $lib_path"
          echo "header_dir: $header_dir"

          # Create expected include directory structure
          mkdir -p include/sherpa-onnx/c-api
          cp "$header_dir"/*.h include/sherpa-onnx/c-api/

          # Use onnxruntime from the build directory
          ort_lib=$GITHUB_WORKSPACE/build-macos-shared/_deps/onnxruntime-src/lib

          # Compile and run
          clang -o version-c-api $GITHUB_WORKSPACE/c-api-examples/version-c-api.c \
            -I include \
            -L "$(dirname $lib_path)" \
            -L "$ort_lib" \
            -l sherpa-onnx-c-api \
            -l onnxruntime \
            -lc++ \
            -Wl,-rpath,"$(dirname $lib_path)" \
            -Wl,-rpath,"$ort_lib"

          ./version-c-api

      - name: Upload xcframework.zip
        uses: actions/upload-artifact@v4
        with:
          name: sherpa-onnx-macos-shared-xcframework
          path: ./build-macos-shared/*.xcframework.zip

      - name: Release xcframework
        if: github.repository_owner == 'k2-fsa' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-macos-shared/*.xcframework.zip
          tag: xcframework

      - name: Release xcframework
        # if: github.repository_owner == 'csukuangfj' && github.event_name == 'push' && contains(github.ref, 'refs/tags/')
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          overwrite: true
          file: ./build-macos-shared/*.xcframework.zip
          repo_name: k2-fsa/sherpa-onnx
          repo_token: ${{ secrets.UPLOAD_GH_SHERPA_ONNX_TOKEN }}
          tag: xcframework

      # https://huggingface.co/docs/hub/spaces-github-actions
      - name: Publish to huggingface
        if: (github.repository_owner == 'csukuangfj' || github.repository_owner == 'k2-fsa') && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 20
          timeout_seconds: 200
          shell: bash
          command: |
            git config --global user.email "csukuangfj@gmail.com"
            git config --global user.name "Fangjun Kuang"

            rm -rf huggingface
            export GIT_CLONE_PROTECTION_ACTIVE=false
            GIT_LFS_SKIP_SMUDGE=1 git clone https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs huggingface

            cd huggingface

            cp -v ../build-macos-shared/*.xcframework.zip ./

            git status
            git lfs track "*.zip"

            git add .

            SHERPA_ONNX_VERSION=v$(grep "SHERPA_ONNX_VERSION" ../CMakeLists.txt | cut -d " " -f 2 | cut -d '"' -f 2)
            git commit -m "upload sherpa-onnx-${SHERPA_ONNX_VERSION} macos-shared xcframework"

            git push https://csukuangfj2:$HF_TOKEN@huggingface.co/csukuangfj2/sherpa-onnx-libs main
