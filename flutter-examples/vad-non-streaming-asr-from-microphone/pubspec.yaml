name: vad_non_streaming_asr_from_microphone
description: >
  VAD + Non-streaming ASR from microphone demo using sherpa-onnx.

publish_to: 'none'

version: 1.13.6

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=2.8.1"

dependencies:
  flutter:
    sdk: flutter

  audioplayers: ^5.0.0
  cupertino_icons: ^1.0.6
  file_picker: ^8.0.0
  path_provider: ^2.1.3
  path: ^1.9.0
  sherpa_onnx: ^1.13.6
  # sherpa_onnx:
  #   path: ../../flutter/sherpa_onnx
  record: ^6.2.0
  url_launcher: 6.2.6
  url_launcher_linux: 3.1.0

flutter:
  uses-material-design: true
